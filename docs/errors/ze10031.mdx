---
title: 'ZE10031: No JWT'
description: 'ZE10031: No JSON Web Token found for authentication'
head:
  - - meta
    - property: og:description
      content: 'ZE10031: No JSON Web Token found for authentication'
---

import { getErrorMessage } from '../../lib/error-helpers.ts';
import { ErrorInfo } from '../../components/errors/info.tsx';
import { Steps } from '@rspress/core/theme';

# {getErrorMessage('ZE10031')}

::: tip Quick Help
The fastest way to resolve this error is to [join our Discord](https://discord.gg/zephyrcloud) and leave a message - our team is ready to help!
:::

<ErrorInfo code="ZE10031" terminal />

This error occurs when Zephyr cannot find a valid JWT token for authentication. This typically means you haven't logged in or your session has expired.

## Debugging the error

<Steps>

### Log in to Zephyr

Authenticate by logging in from the [dashboard](https://app.zephyr-cloud.io) or [Chrome extension](https://chromewebstore.google.com/detail/zephyr-mission-control/liflhldchhinbaeplljlplhnbkdidedn).

### Check for existing credentials

Verify that credentials exist in your Zephyr configuration:

```bash
ls ~/.zephyr
```

### For CI/CD environments

If running in a CI/CD environment, ensure you have set up the `ZEPHYR_TOKEN` environment variable with a valid API token.

</Steps>

