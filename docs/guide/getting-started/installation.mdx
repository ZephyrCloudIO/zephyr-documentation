# Installation

import {PackageManagerTabs} from '@theme';

Adding Zephyr Cloud to a new project using the following command:
<PackageManagerTabs command="install @zephyr-cloud/cli"/>

This will guide you through the setup, asking if you are logged in, and which cloud provider you are using.

