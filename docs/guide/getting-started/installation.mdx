import {Tabs, Tab} from '@theme';

# Installation

## Quick Start Examples

To begin developing new applications, consider exploring these two introductory examples:

* [Create MF App](/guide/getting-started/create-mf-app)
* [Nx Generate Module Federation](/guide/getting-started/nx-mf-app)

## Integrating Zephyr into an Existing Application

Our objective for Zephyr Cloud was to ensure an easy and straightforward setup process. Let's examine how this approach has been implemented:

### 1. Add Zephyr as a dependency

Install the package via NPM:

```sh filename="Terminal"
npm i zephyr-webpack-plugin --save
```

### 2. Setting Up

Wrap your existing configuration

<Tabs values={[{label: 'With Nx'}, {label: 'Without Nx'}]}>
  <Tab>
  For Nx projects that utilize compose plugins, integrating Zephyr is straightforward and requires adding just one more plugin. To ensure proper functionality, Zephyr should be positioned as the last plugin in the composition.
    ```diff filename="webpack.config.js"
    + const {withZephyr} = require('zephyr-webpack-plugin');
    export default composePlugins(
        withNx(),
        withReact(),
        withModuleFederation(config),
    +   withZephyr(),
        (config)=>{
        return config;
      }
    );
    ```

  </Tab>

  <Tab>
  Modify your project's Webpack or Rspack configuration by adding the withZephyr function to the existing setup.

      ```diff filename="webpack.config.js"
      - module.exports = {/* the existing config object */};
      + const {withZephyr} = require('zephyr-webpack-plugin');
      + module.exports = withZephyr()({/* the existing config object */});
      ```

  </Tab>
</Tabs>

### Building the Application

When you build your application, Zephyr will launch your browser and prompt you to log in.

You are now set to develop using Zephyr.