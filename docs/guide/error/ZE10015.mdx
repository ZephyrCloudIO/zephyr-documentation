# ZE10015
This error occurs when a Git username and/or email isn't configured.

Solution:
1.  Set the username:

    ```shell
    git config user.name "John Doe"
    ```
2.  Set the email address:

    ```shell
    git config user.email "john.doe@domain.example"
    ```