# ZE10014

This error occurs when the `origin` remote is not set in your Git repository.

Solution:
1.  Set the `origin` remote, replacing `GIT_URL` with your Git repository's remote URL:

    ```shell
    git remote add origin GIT_URL
    ```